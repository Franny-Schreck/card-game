{NAME} "test-card"
{PLAY-COST} "5"
{SHOP-COST} "2"

{DISPLAYNAME} "I'm not a real card 👉👈"

{DESCRIPTION} "You die. Vrom dissees. Unvair"

{IMAGE} "regierung_smaller"

# {SUPPRESS-DISCARD} "true"

{TAGS}
	"lawless"
	"faith"

{TARGET} "local"

{CONDITION}
	# (> $fl 3)
	(>= (card-count "draw") 3)

{EFFECT}
	(setf - $fl 3)
	(put-card "hand" (pick-card 1 3 "draw" "random"))
	# Choose one of three random cards from dicard pile; Move it to hand
	# (card-put  <cards>)
	# (card-take <count> <destination> <sources> <mode>)
	# (card-pick <count> <out-of-count> <destination> <sources> <mode>)
	# <destination> :- "hand" | "draw" | "discard"
	# <count> :- int
	# <out-of-count> :- int
	# <sources> :- ( "card:*" ){1,*} | "category:*" | self | "hand" | "draw" | "discard"
	# <mode> :- "top" | "bottom" | "random"
